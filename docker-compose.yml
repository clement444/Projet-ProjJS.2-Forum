services:
  forum:
    container_name: Forum
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./forum.db:/app/forum.db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
